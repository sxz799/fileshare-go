
const base = {
    baseUrl: process.env.VUE_APP_BASE_URL,
    upload: "/upload",
    download: "/download",
    config: "/config",
    exist: "/exist",
    del: "/del",
    list: "/list"
}

export default base
